import React from 'react'

const PageHeader = ({title}) => {
    return (
        <section class="page-header">
            <h2 class="title-xxl">{title}</h2>
        </section>
    )
}

export default PageHeader
